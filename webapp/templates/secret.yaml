{{- if .Values.app.database.password }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "webapp.fullname" . }}-secret
  labels:
    {{- include "webapp.labels" . | nindent 4 }}
type: Opaque
data:
  database-password: {{ .Values.app.database.password | b64enc | quote }}
{{- end }}
