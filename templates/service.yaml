apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-multi-env.fullname" . }}
  labels:
    {{- include "webapp-multi-env.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-multi-env.selectorLabels" . | nindent 4 }}
